<%= form_for(@link_component) do |f| %>
  <% if @link_component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link_component.errors.count, "error") %> prohibited this link_component from being saved:</h2>

      <ul>
      <% @link_component.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
